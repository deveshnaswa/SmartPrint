@model SmartPrint.Models.LoginModel
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@Html.ValidationSummary(true)
@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <p>
        <button type="submit">Log In</button>
    </p>

    <h1>@Model.UserEmail</h1>
    <h1>@Model.UserPass</h1>
}
